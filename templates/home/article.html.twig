<div class="article--container">
    <a href="#">
        <img src="{{ picture }}"
             alt="{{ name }}"
             title="{{ name }}" />
    </a>

    <div>
        {% if preName is defined and preName is not null %}
            <span>{{ preName }}</span>
        {% endif %}

        <span>{{ name }}</span>
    </div>
</div>